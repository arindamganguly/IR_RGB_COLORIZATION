# IR_RGB_COLORIZATION
This is a web application, powered by AI, where infrared satellite images are converted into RGB images through an implementation of Pix2Pix GAN. The generator uses a U-net architecture and the discriminator uses PatchGAN. The application is built off of Flask, PyTorch, HTML, CSS, and JavaScript.
